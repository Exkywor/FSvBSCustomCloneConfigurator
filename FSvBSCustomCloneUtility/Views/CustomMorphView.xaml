<UserControl x:Class="FSvBSCustomCloneUtility.Views.CustomMorphView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FSvBSCustomCloneUtility.Views"
      xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
      xmlns:fa5="http://schemas.fontawesome.com/icons/"
      xmlns:cal="http://www.caliburnproject.org"
      mc:Ignorable="d" 
      BorderThickness="0"
      d:DesignHeight="450" d:DesignWidth="800">


    <Grid Grid.Row="1" Margin="{adonisUi:Space}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" FontWeight="Bold" Margin="{adonisUi:Space}">
                STEPS
        </TextBlock>

        <!-- FSvBS file -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="3">
            <TextBlock>
                1. Select the BioD_FSvBS.pcc file located in the DLC folder.
            </TextBlock>
            <StackPanel Margin="0, 10" Orientation="Horizontal">
                <TextBox Text="{Binding FSvBSFile, Mode=OneWay}" ToolTip="BioD_FSvBS_Dummies.pcc file." IsReadOnly="True" Width="500" Height="20"/>
                <Button x:Name="FSvBSFileButton">Browse</Button>
            </StackPanel>
            <TextBlock x:Name="WarningFSvBSFile" Foreground="Red"/>
        </StackPanel>

        <!-- Male headmorph -->
        <TextBlock Grid.Row="3" Grid.ColumnSpan="3">
                2. Select the .ron file containing the male headmorph you wish to apply. Make sure to use a headmorph for the correct game. You can skip this step if you don't want to use a male headmorph.
        </TextBlock>
        <TextBox Text="{Binding RonMFile, Mode=OneWay}"  ToolTip="Male .ron file." IsReadOnly="True" Grid.Row="4" Grid.Column="0"/>
        <Button x:Name="RonMFileButton"  Content="Browse" Command="" Grid.Row="4" Grid.Column="1"/>
        <fa5:ImageAwesome Icon="Solid_CheckCircle" Foreground="#FFFFFF" Grid.Row="4" Grid.Column="2"/>

        <!-- Female headmorph -->
        <TextBlock Grid.Row="5" Grid.ColumnSpan="3">
                3. Select the .ron file containing the female headmorph you wish to apply. Make sure to use a headmorph for the correct game. You can skip this step if you don't want to use a male headmorph.
        </TextBlock>
        <TextBox Text="{Binding RonFFile, Mode=OneWay}"  ToolTip="Female .ron file." Grid.Row="6" Grid.Column="0"/>
        <Button x:Name="RonFFileButton" Content="Browse" Command="" Grid.Row="6" Grid.Column="1"/>
        <fa5:ImageAwesome Icon="Solid_CheckCircle" Foreground="#FFFFFF" Grid.Row="6" Grid.Column="2"/>

        <!-- Resources -->
        <TextBlock Grid.Row="7" Grid.ColumnSpan="3">
                4. Select the .pcc files that contain modded hairs or textures that your headmorph uses. Make sure they are for the correct game. You can skip this step if you don't use any mod for the headmorph, or if the textures are applied via .mem.
        </TextBlock>
        <TextBox x:Name="ResourcesText" Text=""  ToolTip="BioD_FSvBS_Dummies.pcc file." Grid.Row="8" Grid.Column="0"/>
        <Button x:Name="ResourcesBrowse" Content="Browse" Command="" Grid.Row="8" Grid.Column="1"/>
        <fa5:ImageAwesome Icon="Solid_CheckCircle" Foreground="#FFFFFF" Grid.Row="8" Grid.Column="2"/>

        <!-- Apply -->
        <TextBlock Grid.Row="9" Grid.ColumnSpan="3">
                5. Apply the headmorphs!
        </TextBlock>
        <Button x:Name="ApplyButton" IsEnabled="{Binding IsValid}" Content="Apply" Command="" Grid.Row="10" Grid.Column="0"/>
    </Grid>
</UserControl>
