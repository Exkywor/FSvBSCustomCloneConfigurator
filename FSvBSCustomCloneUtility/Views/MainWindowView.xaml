<Window x:Class="FSvBSCustomCloneUtility.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FSvBSCustomCloneUtility"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:viewmodels="clr-namespace:FSvBSCustomCloneUtility.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="FemShep v BroShep Utility" Height="450" Width="720"
        WindowStartupLocation="CenterScreen"
        MinWidth="720"
        MinHeight="450">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="50" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Grid.Background>
                <ImageBrush ImageSource="../images/main_banner.png" Stretch="UniformToFill"/>
            </Grid.Background>

            <Image Grid.Column="1" Source="../images/logo.png" Width="400" Margin="{adonisUi:Space}"/>
        </Grid>

        <TextBlock Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" FontSize="14" FontWeight="Bold" Margin="0,20,0,0">
                STEPS
        </TextBlock>

        <!-- Game Target -->
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="{adonisUi:Space}">
            <TextBlock TextWrapping="Wrap">
                1. Select the target game. The FemShep v BroShep mod must be installed before using this tool.
            </TextBlock>
            <Grid Margin="0, 10, 0, 5" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Button x:Name="TargetME3" HorizontalAlignment="Center" Width="200" Grid.Column="0"
                        Background="{Binding ME3ButtonColor}"
                        Content="ME3"/>
                <Button x:Name="TargetLE3" HorizontalAlignment="Center" Width="200" Grid.Column="2"
                        Background="{Binding LE3ButtonColor}" 
                        Content="LE3"
                />
            </Grid>
        </StackPanel>

        <ContentControl Grid.Row="3" Grid.Column="1" x:Name="CustomMorph" />

        <ContentControl Grid.Row="4" Grid.Column="1" x:Name="ConditionalsControl" />
    </Grid>
</Window>
