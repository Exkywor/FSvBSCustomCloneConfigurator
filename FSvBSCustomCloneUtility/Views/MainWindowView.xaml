<Window x:Class="FSvBSCustomCloneUtility.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FSvBSCustomCloneUtility"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:viewmodels="clr-namespace:FSvBSCustomCloneUtility.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="FemShep v BroShep Utility" Height="450" Width="800">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Grid.Background>
                <ImageBrush ImageSource="../images/main_banner.png" Stretch="UniformToFill"/>
            </Grid.Background>

            <Image Grid.Column="1" Source="../images/logo.png" Margin="{adonisUi:Space}"/>
            <StackPanel Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label Margin="0,0,0,10" FontSize="15" Content="Target Game" HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton Name="ME3Checked" FontSize="15" Margin="10, 0" GroupName="MEGame" Content="ME3" />
                    <RadioButton Name="LE3Checked" FontSize="15" Margin="10, 0" GroupName="MEGame" Content="LE3" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <ContentControl Grid.Row="1" Grid.Column="1" x:Name="CustomMorph" />

        <DockPanel Grid.Row="2" Grid.Column="1">
            <TextBlock >Toggable buttons</TextBlock>
        </DockPanel>
    </Grid>
</Window>
